# Comma-separated origins allowed to call this service from the browser.
ALLOWED_ORIGINS=http://localhost:5173

# Coqui TTS model id (English default). Replace once a Kinyarwanda voice exists.
COQUI_MODEL=tts_models/en/ljspeech/tacotron2-DDC
